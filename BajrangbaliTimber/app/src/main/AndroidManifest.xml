<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bajrangbalitimber">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bajrangbali_photo3"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher3"
        android:supportsRtl="true"
        android:theme="@style/Theme.BajrangbaliTimber">
        <activity android:name=".SizeWood"></activity>
        <activity android:name=".WoodLog" />
        <activity android:name=".HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>